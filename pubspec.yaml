name: x_crop
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0-174.3.beta <4.0.0'

dependencies:
  collection: ^1.18.0
  flutter:
    sdk: flutter
  signals: ^2.1.10
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
